$(document).ready(function() {
    var gameHeight = 500,
        gameWidth = 500,
        
        ballName = "ball",
        ballSize = 5,
        ballSpeed = 1,
        
        leftPaddle = "leftPaddle",
        rightPaddle = "rightPaddle",
        paddleLength = 40,
        paddleWidth = 5;
});

function moveBall(ball, speed) {
    var 
    if (speed > 0) {
        
    } else {
        
    }
}